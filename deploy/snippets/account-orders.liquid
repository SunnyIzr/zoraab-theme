<h3>Order History</h3>
<table class='table'>
  <thead>
    <tr>
      <th width="16.6%">Order #</th>
      <th width="16.6%">Date</th>
      <th class='hidden-xxs' width="16.6%">Ship To</th>
      <th width="16.6%">Total</th>
      <th class='hidden-xxs' width='16.6%'>Status</th>
      <th width='16.6%'></th>
    </tr>
  </thead>
  <tbody>
    {% for order in customer.orders %}
      <tr>
        <td>{{ order.name }}</td>
        <td>{{ order.created_at | date: "%b %d, %Y" }}</td>
        <td class='hidden-xxs'>{{ order.shipping_address.name }}</td>
        <td>{{ order.total_price | money }}</td>
        <td class='hidden-xxs'>{% if order.fulfillment_status =="fulfilled" %}Shipped{%else%}
            {% if order.payment_status =="Refunded" %}Refunded {%else%} Processing {%endif%}{% endif %}</td>
        <td><a href='#' data-toggle="modal" data-target="#{{order.id}}">View Order</a></td>
      </tr>            
    {%endfor%}
  </tbody>
</table>